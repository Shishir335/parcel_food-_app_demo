import 'package:flutter/material.dart';

class AppWrawer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Drawer(
      child: Column(
        children: [],
      ),
    );
  }
}
